---
description: 完成功能或修改后必须提交、撰写说明并推送到 GitHub
alwaysApply: true
---

# Git：提交与推送

在完成**新增功能**或**代码修改**且变更已保存后，助手必须主动执行版本控制闭环，不要只改文件而不提交。

## 必须执行的步骤

1. **暂存**：`git add` 纳入本次相关的全部变更（勿遗漏与功能配套的配置、测试）。
2. **提交**：`git commit`，消息须**描述清楚做了什么、为何需要**（推荐前缀：`feat:` / `fix:` / `refactor:` / `docs:` / `test:` / `chore:`，与仓库 AGENTS.md 分支前缀一致）。
3. **推送**：`git push` 到远程（GitHub）。若当前分支无 upstream，先 `git push -u origin <branch>`。

## 约束与例外

- 若用户明确说「不要提交 / 不要推送」，则以用户当前指令为准。
- 本仓库约定 PR 面向 `dev`、避免对 `main`/`dev` 直推；在功能分支上提交并 push 到 origin 即可。
- CI 较重时：将**同一逻辑任务**内的修改放在**一次提交**里；不要为了「每个文件一条 commit」而强行拆分，但也不要把无关改动混进同一提交。

## 禁止

- 使用空泛的提交说明（如 `update`、`fix`、`wip` 且无上下文）。
- 在已完成可交付改动的情况下，以「你可以自己运行 git」代替实际执行提交与推送（除非环境无 `git_write` 或用户禁止）。
